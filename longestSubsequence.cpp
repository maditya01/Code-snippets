//Longest Subsequence with some property.
// Subsequence ends at ith index.
 //Base case -> each element is itself longest with ending at itself.
// what dp state define 
//One-d DP 2-d Dp may be with some constraint 
for(int i=1;i<n;i++){
 
  for(int ind=i-1;ind>=0;ind--){
  //apply some property.
  }
  
}
